---
import LayoutProyecto from "../../../layouts/LayoutProyecto.astro";
const allSounds = await Astro.glob('./src/*.mp3');

let sounds =  [];

allSounds.map( (sound, i) => {
  sounds[i] = { 
    url: sound.default, 
    name: sound.default.split('/').pop().replace('.mp3', '')
  } //me da un array con arrays con la url y el nombre del archivo
})

/* console.log(sounds) */


---

<LayoutProyecto title="Día 9" subtitle="Sound Board">
<div class="container">
  {
    sounds.map((sound, i) => (
      <audio id={sound.name} src={sound.url} class="sound"></audio>
    ))
  }

  <div id="buttons">
    {
      sounds.map((sound, i) => (
        <button class="btn">
          {sound.name} 
        </button>
      ))
    }
  </div>

</div>
</LayoutProyecto>

<style>
    .container {
      display: grid;
      height: 70vh;
      place-items: center;
    }

    #buttons {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      height:fit-content;
      gap: 2rem;
    }
    .btn {
      background-color: var(--naranja-claro);
      padding: 1rem;
      border-radius: 50%;
      width: 180px;
      height: 180px;
      overflow: hidden;
      text-overflow: ellipsis;
      text-transform: capitalize;
      color: #fff;
      font-size: xx-large;
      font-weight: 600;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      box-shadow: 0px 3px 1px var(--naranja-oscuro);
      transition: background 0.1s ease-in-out;
      
      &:hover {
        background-color: var(--naranja);
      }

      &:active {
        background-color: var(--naranja-oscuro);
        box-shadow: none;
        transform: translateY(3px);
      }
      
    }

    .btn:before {
        content: '\25BA';
        font-size: xxx-large;
        text-align: center;
        text-overflow: ellipsis;
    }


  @media (max-width: 650px) {
    #buttons {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      height:fit-content;
      gap: 1rem;
    }

    .btn {
      font-size: x-large;
      width: 140px;
      height: 140px;
    }

    .btn:before {
        font-size: xx-large;
    }

  }

</style>

<script>
  const body = document.querySelector('.container');

  


</script>